{
  "buildCommand": "cd client && yarn install && npx vite build --config vite.config.js",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    { "source": "/(.*)", "destination": "/" }
  ]
}
